// components/NotAllowed.js
import React from 'react';

const NotAllowed = () => {
    return (
        <div className="not-allowed">
            <h1>Access Denied</h1>
            <p>You do not have permission to access this page.</p>
        </div>
    );
};

export default NotAllowed;
